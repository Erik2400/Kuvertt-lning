javascript: (() => {    if (!window.__xhrCounterInstalled) {        alert("Tælleren skal først køre");        return;    }    if (window.__xhrtimedif) {        alert("Tidsforskellen bliver allerede målt");        return;    }    window.__xhrtimedif = true;    const co = document.getElementById("Counter1234");    const timediv = document.createElement("div");    co.parentNode.insertBefore(timediv, co.nextSibling);    const observer = new MutationObserver(() => {        const now = new Date();        const prev = localStorage.getItem("tidsforskel");        localStorage.setItem("tidsforskel",  String(now.getTime()));        if (prev === null) {return;}        const difference = (now.getTime() - Number(prev)) / 1000;        timediv.textContent = `Det tog ${difference.toFixed(1)} sekunder`;    });    observer.observe(co, {         childList: true,         characterData: true,         subtree: true     });})();